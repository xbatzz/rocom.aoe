# 变更记录

## 2026-08-08

- `/pvp-lite` 原速度栏目升级为双方当前构筑六项实战属性对比，逐项展示生命、物攻、魔攻、物防、魔防、速度与双方差值。
- 移除独立 `/pvp` 详细参数和 `/stats` 实战属性页面，清理首页、宠物详情、侧边栏与对战助手中的相关入口；实战属性计算继续由对战助手、配队和伤害模块复用。

## 2026-08-02

- 修复 `/skills` 中 `#7000341` 及后续新技能缺少描述、能耗、威力和图标的问题；`PetSkillIndex.json.skills` 现由 `SKILL_CONF` 同步生成完整技能详情，`moves.json` 继续作为旧技能整理信息与同名合并来源。
- 修正 FModel 技能图标导入路径：严格按 `SKILL_CONF.icon` 从 `BattleUI/Raw/Atlas/SkillIcon` 导入正方形素材，不再误用 `Common/Icon/SkillBase` 的 `512×256` 横向插画；新增正方形校验和 `--overwrite-skills` 定向覆盖参数，并升级 PWA 图片缓存版本以淘汰旧横图。

## 2026-08-01

- 重组全局信息架构：侧边栏由 15 个平铺入口收敛为核心工具、资料与收集、培育和数据管理；首页次级工具同步按使用场景分组。
- 统一对战入口：`/pvp-lite`、`/pvp`、`/stats` 共用快速决策、详细参数、实战属性模式导航，详细版不再占用独立一级导航。
- 统一培育入口：`/breeding`、`/incubate`、`/egggroup` 共用配种判断、孵蛋 / 查蛋、蛋组关系模式导航，侧边栏只保留一个培育工具入口。
- 宠物详情增加独立战斗资料栏目，手机首屏压缩头像与图鉴任务，并提供携带当前精灵进入实战属性计算器的入口。
- 将原“表格”重命名为“高级筛选”；桌面保留可排序表格，手机改用精灵结果卡并默认收起组合筛选。
- 图鉴进度导入导出明确为“仅图鉴”，完整备份统一引导至数据管理；新增可复用模式导航和手机筛选结果卡组件。
- 完成核心页面信息架构整改：首页压缩移动端英雄区与队伍摘要，属性页改用紧凑主属性网格和移动端副属性选择器，数据管理页将备份操作前移到首屏。
- `/encyclopedia` 与 `/skills` 改为搜索优先，移动端默认收起筛选与排序；顶部统计压缩为单行，桌面端继续保留完整筛选栏。
- `/team` 手机端点击槽位后直接打开全屏构筑面板，避免在六个槽位后寻找候选列表；桌面端继续使用右侧粘性面板。
- `/pvp` 与 `/stats` 增加渐进展示：未完成宠物选择时不再渲染空摘要、配置和结果区；同步修正详细 PVP 的纸面伤害能力说明。
- 为图鉴、技能、配队、详细 PVP、实战属性和数据管理补齐语义化一级标题，统一核心页面标题结构。
- 重构 `/pvp-lite` 信息架构：手机端使用五项图标标签，桌面端使用横向标签栏，速度、克制、联防、伤害与资料一次只展示一个栏目；移除栏目内二次折叠，并补齐 `tablist` / `tab` / `tabpanel`、方向键及 Home / End 键盘操作。
- 压缩 `/pvp-lite` 双方选择区，将临时构筑收进精灵卡设置；空队伍增加配队入口，手动选择我方补回“无配置”，移动端底部操作栏退出悬浮层，避免遮挡速度和伤害内容。
- 调整 `/pvp-lite` 结论口径，明确区分“当前配置速度”与对方满速参考，并将属性结论限定为双方本系最高倍率比较，避免把轻量提示误解为完整对位判断。
- 优化 `/pvp-lite` 双向伤害入口：无装备技能时直接推荐当前攻击方可学习的高威力技能，搜索仅覆盖当前精灵技能池，并允许在伤害栏目内切换临时构筑。
- `/pvp-lite` 新增双方种族值与特性资料面板，区分特性加载中、加载失败、暂无数据和正常结果；同步改善空状态、搜索结果辨识度、深色模式对比度及卡片视觉层级。

## 2026-07-31

- `pvp-lite` 新增本地对战问答，可通过文字或浏览器语音询问速度比较、六维种族值、当前实战属性与生命、属性克制、最强本系打点、一击判断、联防换人、最快配置及最高伤害技能；快捷提示精简为速度、我方进攻和对方进攻三个核心场景，技能问答会同步切换伤害模块并展示完整计算结果。
- `pvp-lite` 伤害技能模块新增“我方攻击对方 / 对方攻击我方”切换；切换到对方后可搜索对方精灵可学习的伤害技能，并反转双方构筑、生命条件和攻防参数计算对我方伤害。
- `pvp-lite` 新增陨星虫捕捉球特性选项，默认以紧凑按钮收起，点击后可查看完整配置；普通球、高级球、国王球与绝缘球的速度增益会计入实战速度和先手判断，手动选择陨星虫时默认使用常见的极速绝缘球配置。
- 优化首页首屏：移除“优先、后续、规划中”等开发说明式表达，改为对战分析与配队快捷操作，并展示当前激活队伍的阵容进度。
- 新增 `/data-management` 数据管理页，可一次导出、校验和导入全部配队、图鉴进度与主题设置；导入支持按更新时间合并或完全替换，并在完成后刷新应用内存状态。
- 新增 `docs/USER_DATA.md`，记录浏览器存储键、完整备份格式、PC/Mac/iOS 迁移步骤、合并规则与当前非云同步边界。

## 2026-07-26

- 新增 `docs/SEASON_DATA_UPDATE.md`，记录 FModel 补丁层与本地化校验、赛季精灵识别、17 张原始表、生成流程、形态图片命名和验收清单；新增 `yarn import:fmodel-icons` 自动补齐 FModel 导出的精灵与技能 WebP。
- 新增全局浅色 / 暗色主题开关，默认保持暗色并记住用户选择；统一 `/pvp-lite` 与其他页面的主题表现。
- `/pvp-lite` 新增双方一击威力线与当前生命滑块，可在 0%-100% 生命范围内同步反推本系与非本系等倍技能一击所需的最低基础威力，并在 `damageCalculator` 中补充可复用的反推函数。

## 2026-07-09

- 简化 `/pvp-lite` 默认信息层级，将速度线、属性细节和联防候选列表下沉到折叠区，默认只保留核心摘要。
- `/pvp-lite` 新增双方临时配置预设，支持当前构筑、无配置、极限攻击、极速和极限生命，用于页面内快速查看速度线与纸面伤害变化。
- 修正属性倍率上限，双弱点由 4x 改为 3x，统一使用 0.25 / 0.5 / 1 / 2 / 3 倍率口径。
- 修复 `/team` 构筑预设的性格选择逻辑，极速与极限攻击会同时匹配正向和负向属性，避免降低宠物主攻项。
- `/team` 当前槽位构筑配置新增构筑预设快捷按钮，支持极限攻击、极速、极限生命和清空个体，快速设置性格与个体值。

## 2026-07-08

- 重排 `/pvp-lite` 为手机游戏助手式主界面，强化双方对位舞台、主结果卡和最高推荐技能卡，辅助信息与详细计算下沉。
- 新增 `/pvp-lite` 移动端轻量对战助手，复用当前队伍、属性倍率、实战速度线、纸面伤害和联防候选，并在首页与侧边栏新增入口，原 `/pvp` 详细版保留。
- 修复 `/pvp` 纸面伤害无法读取当前队伍构筑技能的问题，兼容 `/team` 保存的 `moveIds` 并通过 `PetSkillIndex` 映射宠物技能 ID。
- 修正 `/pvp` 纸面伤害公式口径：默认等级改为 60，本系倍率改为 1.25，并将显示威力与预计伤害取整顺序对齐 rocopvp battle-use-guide。
- 新增 `src/lib/damageCalculator.ts`，复用 `statCalculator` 的实战属性面板，提供固定威力物理 / 魔法攻击技能的纸面伤害估算。
- `/pvp` 新增纸面伤害计算 v0.1，只支持我方技能打对方，优先读取当前激活队伍槽位保存的技能、性格和个体值，并排除状态、防御、无固定属性和无固定威力技能。

## 2026-07-07

- `/pvp` 属性倍率卡片新增我方抗性候选功能，点击对方属性后默认读取当前激活队伍并展示 0.5x / 0.25x 联防参考。
- 配队页新增多配队管理 v0.1，支持新建、切换、重命名、复制和删除队伍，并将 `teamStorage` 升级到 `rocom.team-builder.v2` 多队伍结构，兼容旧 `rocom.team-builder.v1` 单队伍数据；`/pvp` 继续通过 helper 读取当前激活队伍。
- 调整 `/pvp` 和 `/stats` 宠物选择器默认展示逻辑，无搜索词时不再显示编号前 50，改为搜索驱动并保留当前已选展示。
- 优化 `/pvp` 和 `/stats` 宠物选择器体验，搜索选择后不再跳回默认前列，并新增当前已选展示、候选高亮和空状态。
- `/pvp` 对方速度区域新增常见速度线速览，展示满速、满个体、无速、减速 4 种配置的实战速度估算。
- 配队页构筑配置新增六项个体值配置，随槽位保存到 `rocom.team-builder.v1`，并兼容旧队伍数据和旧分享链接。
- `/pvp` 从已保存配队选择我方宠物时，自动读取槽位速度个体值和性格速度修正，用于实战速度线参考且不反写配队。
- `/pvp` PVP 对位助手升级到 v0.2，接入实战速度线计算，支持双方速度个体值与速度性格修正，并继续避免伤害计算和胜率预测。
- 新增 `/stats` 实战属性计算器 v0.1，并新增可复用 `src/lib/statCalculator.ts`，支持种族值、个体值和性格修正按分步四舍五入规则计算 PVP 实战属性。
- 新增 `/pvp` PVP 对位助手 v0.1，支持从已保存配队选择我方宠物、手动选择双方宠物，并展示属性倍率、速度差、种族值和轻量风险提示。
- 移除主内容滚动容器的平滑滚动效果，路由切换回顶部时改为立即跳转，统一 iPhone Safari、PWA 和桌面浏览器表现。

## 2026-07-06

- 修复 iPhone Safari/PWA 在内部滚动容器中切换路由后保留旧滚动位置的问题，跨页面导航会回到顶部。
- 统一 `/skills` 顶部标题、统计卡和筛选区风格，使技能页与图鉴页的页面结构更一致。
- 技能查询页新增分页，支持每页 24 / 48 / 72 条切换，搜索和筛选变化后自动回到第 1 页。
- 移除首页友情链接区域和侧边栏 GitHub / 赞助入口，使个人版入口更简洁。
- 移除原项目 Google AdSense 广告脚本、广告组件和首页/孵蛋/配种/表格页广告引用，改善个人版加载与 PWA 离线体验。
- 修复 PWA app-shell 未明确缓存 `/index.html` 导致的离线启动白屏，升级缓存版本到 `rocom-pwa-v2-*`，并补充 Vercel SPA fallback 配置。
- 新增离线优先 PWA 第一版：补充 manifest、主屏幕图标、service worker 注册、核心路由和核心 JSON 缓存策略。
- 新增 `docs/PWA.md`，记录 iPhone/iPad 添加到主屏幕、离线范围、预缓存数据、runtime cache 和更新策略。
- 新增 `docs/DESIGN_SYSTEM.md`，沉淀个人版 UI 风格、首页入口、卡片、标签、查询页、移动端、PVP 页面和开发约束。

## 2026-07-05

- 统一首页核心入口标题为属性查询、图鉴、技能查询与 PVP 工具 Coming Soon。
- 侧边栏新增正式“技能”入口，指向 `/skills`，并保留原有功能入口。
- 新增 `docs/CURRENT_STATUS.md`，记录当前个人版项目状态、核心页面、暂不做事项和后续建议。
- 合并 `/skills` 中同名但不同 ID 的重复技能，保留 `moves.json` 详情并从 `PetSkillIndex` / 宠物详情补充图标与补充来源 ID 搜索。
- 修正 `/skills` 技能数据合并策略，以 `PetSkillIndex.json.skills` 为技能目录基础，再用 `moves.json` 补充详情，并从宠物详情技能对象补齐图标。
- 修复 `/skills` 技能卡片图标显示，复用宠物详情页同一套 `SkillIcon.vue` 与 `public/assets/webp/items/{icon_id}.webp` 图标来源。
- 新增 `/skills` 技能查询页第一版，支持技能名称、ID、描述、属性和分类查询，仅展示技能本身信息。
- 新增 `src/features/skills/skillAdapter.ts`，为后续独立技能查询页提供数据适配、搜索和筛选基础能力。
- 双属性模式新增“打击面”区域，分别展示两个技能属性的 2x 克制目标与合并覆盖。
- 属性关系页过滤“首领”机制类型，避免将其作为普通战斗属性参与查询和图谱展示。
- 属性关系页新增双属性防守查询，支持 3x、2x、0.5x、0.25x 与普通承伤分组。
- 属性关系页改为四象限关系卡片优先展示，ECharts 圆形图谱保留并下移为辅助视图。
- Phase 1：首页改造为个人版“洛克王国世界战斗资料助手”入口。
- 首页核心入口调整为属性克制、图鉴搜索、技能搜索、PVP 工具 Coming Soon。
- 原项目功能保留在“更多工具”区域，包括配队、配种、孵蛋/查蛋、星图、宠物表格、道具、图鉴进度。
- 新增 `src/features/my-home/MyHomeDashboard.vue`，降低后续首页改造与上游同步冲突。
